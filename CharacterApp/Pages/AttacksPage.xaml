<Page x:Class="CharacterApp.Pages.AttacksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="{DynamicResource AttacksHeader}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource AttacksHeader}"
                   FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="4,0,4,12"/>

        <ContentControl x:Name="SearchBarHost" Grid.Row="1" Margin="0,0,0,4"/>

        <DataGrid x:Name="AttacksGrid"
                  Grid.Row="2"
                  Style="{StaticResource SkillsGridStyle}"
                  RowStyle="{StaticResource SkillsRowStyle}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  ItemsSource="{Binding Attacks}"
                  Margin="0,0,0,8"
                  Background="Transparent"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  IsReadOnly="False">

            <DataGrid.Columns>

                <!-- 1. Тип атаки -->
                <DataGridTemplateColumn Header="Тип" Width="160" SortMemberPath="AttackType">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding AttackType}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       VerticalAlignment="Top" Padding="8,6"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding AttackType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource CellTextBox}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- 2. Описание -->
                <DataGridTemplateColumn Header="Описание" Width="*" MinWidth="200" SortMemberPath="Description">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Description}"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextBrush}"
                                       VerticalAlignment="Top" LineHeight="17" Margin="0,2,0,2" Padding="8,6"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource CellTextBox}"
                                     AcceptsReturn="False" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- 3. Статус (круг/крест) -->
                <DataGridTemplateColumn Header="●" Width="80" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton
                                IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Template="{StaticResource StatusToggleTemplate}"
                                Click="StatusToggle_Click"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Width="36" Height="36" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{DynamicResource BtnAddSkill}"    Click="BtnAdd_Click"    Margin="6" Padding="12,7" Style="{DynamicResource PrimaryButton}"/>
            <Button Content="{DynamicResource BtnRemoveSkill}" Click="BtnRemove_Click" Margin="6" Padding="12,7"/>
            <Button Content="{DynamicResource BtnResetSkills}" Click="BtnReset_Click"  Margin="6" Padding="12,7" Style="{DynamicResource DangerButton}"/>
        </StackPanel>
    </Grid>
</Page>
