<Page x:Class="CharacterApp.Pages.ActiveSkillsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:CharacterApp.Pages"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      Title="Активные навыки">

    <Page.Resources>
        <x:Array Type="{x:Type sys:String}" x:Key="Categories">
            <sys:String>Классовый (К)</sys:String>
            <sys:String>Расовый (P)</sys:String>
        </x:Array>

        
        

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        

        
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource ActiveSkillsHeader}"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="4"/>

        <!-- IMPORTANT: ItemsSource задаётся через привязку к DataContext.Skills.
             Не добавляй вручную <DataGrid.Items> в XAML — приведёт к ошибке. -->
        <ContentControl x:Name="SearchBarHost" Grid.Row="1" Margin="0,0,0,4"/>

        <DataGrid x:Name="SkillsGrid"
                  Grid.Row="2"
                  Style="{StaticResource SkillsGridStyle}" RowStyle="{StaticResource SkillsRowStyle}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  ItemsSource="{Binding Skills}"
                  Margin="0,8,0,8"
                  Background="Transparent"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  IsReadOnly="False">

            <DataGrid.Columns>
                <!-- Column 1: название навыка (редактируемое) -->
                <DataGridTemplateColumn Header="Название" Width="200" SortMemberPath="SkillName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding SkillName}" Foreground="{DynamicResource TextBrush}"
                                       VerticalAlignment="Center" FontWeight="SemiBold" Padding="6,4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding SkillName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource CellTextBox}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- Column 2: выбор категории -->
                <DataGridTemplateColumn Header="Категория" Width="180" SortMemberPath="CategoryDisplay">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CategoryDisplay}" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{StaticResource Categories}"
                                      SelectedIndex="{Binding CategoryIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Background="Transparent"
                                      Foreground="{DynamicResource TextBrush}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- Column 3: описание/название -->
                <DataGridTemplateColumn Header="Описание" Width="*" MinWidth="240" SortMemberPath="Description">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center" Padding="6,4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource CellTextBox}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- Column 4: символ (круг или крест) -->
                <DataGridTemplateColumn Header="●" Width="90" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton IsChecked="{Binding IsActiveSymbol, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Template="{StaticResource StatusToggleTemplate}"
                                          Click="StatusToggle_Click"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Width="36" Height="36" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ToggleButton IsChecked="{Binding IsActiveSymbol, Mode=TwoWay}"
                                          Template="{StaticResource StatusToggleTemplate}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Width="36" Height="36" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="BtnAdd" Content="{DynamicResource BtnAddSkill}" Click="BtnAdd_Click" Margin="6" Padding="10,6" Style="{DynamicResource PrimaryButton}"/>
            <Button x:Name="BtnRemove" Content="{DynamicResource BtnRemoveSkill}" Click="BtnRemove_Click" Margin="6" Padding="10,6" />
            <Button x:Name="BtnReset" Content="{DynamicResource BtnResetSkills}" Click="BtnReset_Click" Margin="6" Padding="10,6" Style="{DynamicResource DangerButton}"/>
        </StackPanel>
    </Grid>
  </ScrollViewer>
</Page>
