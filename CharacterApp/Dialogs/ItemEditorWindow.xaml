<Window x:Class="CharacterApp.Dialogs.ItemEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование предмета"
        Height="625" Width="581"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Редактировать предмет"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="4,2,4,8"
                   Foreground="{DynamicResource TextBrush}"/>

        <!-- Card -->
        <Border Grid.Row="1"
                Style="{StaticResource CardStyle}"
                Padding="12"
                CornerRadius="8"
                SnapsToDevicePixels="True"
                MinHeight="360">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Labels and fields -->
                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Имя:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TbName" Margin="6,4" Height="30" />

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Рарность:"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="TbRarity" Margin="6,4" Height="30" />

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Text="Статы:"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="TbStats" Margin="6,4" AcceptsReturn="True" Height="72" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Text="Эффекты:"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="TbEffects" Margin="6,4" AcceptsReturn="True" Height="72" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="Картинка:"/>
                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="6,4">
                    <TextBox x:Name="TbImagePath" Width="260" IsReadOnly="True" VerticalContentAlignment="Center" Height="30" Margin="0,5,5,5"/>
                    <Button Content="..." Width="36" Margin="6,0,0,0" Click="BtnBrowse_Click"/>
                </StackPanel>

                <!-- Preview (заполняется кодом) -->
                <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                        Margin="0,10,0,0" Padding="6"
                        BorderBrush="{DynamicResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Background="{x:Null}"
                        Height="110">
                    <Image x:Name="PreviewImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="96" Margin="6" Click="BtnOK_Click"/>
            <Button Content="Отмена" Width="96" Margin="6" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
