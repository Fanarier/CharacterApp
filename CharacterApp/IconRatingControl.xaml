<UserControl x:Class="CharacterApp.IconRatingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="controlRoot"
             MinHeight="30">
    <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding IconIndices, ElementName=controlRoot}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Width="24" Height="24" Margin="2" Tag="{Binding}" MouseDown="Image_MouseDown_Handler">
                        <Image.Source>
                            <MultiBinding Converter="{StaticResource RatingIconMultiConverter}">
                                <Binding Path="."/>
                                <Binding ElementName="controlRoot" Path="Value"/>
                                <Binding ElementName="controlRoot" Path="ActiveIcon"/>
                                <Binding ElementName="controlRoot" Path="InactiveIcon"/>
                            </MultiBinding>
                        </Image.Source>
                    </Image>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
